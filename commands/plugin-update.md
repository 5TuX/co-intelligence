---
description: Update any installed Claude Code plugin to the latest version from its marketplace. Defaults to co-intelligence. Works around the stale cache bug.
---

This command is provided by the co-intelligence plugin. Invoke the `co-intelligence:plugin-update` skill using the Skill tool, passing through any arguments the user provided.
